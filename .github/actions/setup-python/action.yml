name: Setup Python
description: Set up the shared GitHub Actions Python runtime from the repo version policy.

runs:
  using: composite
  steps:
    - uses: actions/setup-python@v6
      with:
        python-version-file: ".python-version"
        cache: "pip"
        cache-dependency-path: apps/server/pyproject.toml
