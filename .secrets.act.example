# Copy to .secrets.act and fill in real values.
# Never commit .secrets.act — it is git-ignored.
#
# Currently no secrets are required for the CI workflow.
# Add entries here if future jobs need them (e.g. GITHUB_TOKEN).
