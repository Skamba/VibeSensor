services:
  vibesensor-server:
    build:
      context: .
      dockerfile: infra/docker/server.Dockerfile
    environment:
      - VIBESENSOR_SERVE_STATIC=1
    network_mode: host
    volumes:
      - ./apps/server/data:/app/apps/server/data
    restart: unless-stopped
