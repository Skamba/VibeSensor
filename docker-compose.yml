services:
  vibesensor-server:
    build:
      context: .
      dockerfile: pi/Dockerfile
    environment:
      - VIBESENSOR_SERVE_STATIC=1
    ports:
      - "8000:8000"
      - "9000:9000/udp"
      - "9001:9001/udp"
    volumes:
      - ./pi/data:/app/pi/data
    restart: unless-stopped
