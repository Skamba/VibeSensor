# Docker overrides — only fields that differ from defaults.
# Uses local relative paths inside the container working directory.

logging:
  metrics_log_path: pi/data/metrics.jsonl

storage:
  clients_json_path: pi/data/clients.json
