[pytest]
markers =
    selenium: UI tests requiring selenium/webdriver support
    e2e: docker-based end-to-end tests
    long_sim: longer simulated-run tests (>20 s of synthetic data)
    smoke: minimal critical path checks
